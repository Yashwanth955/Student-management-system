   // tailwind.config.js
   module.exports = {
    mode: 'jit', // Enable JIT mode
    purge: ['./frontend/public/**/*.html'], // Adjust the path to your HTML files
    theme: {
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
  };